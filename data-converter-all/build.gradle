dependencies {
    // 引用除了本模块与根模块之外的所有模块
    rootProject.allprojects.stream().filter({
        it != rootProject && it != project
    }).forEach({
        compile it
    })

    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'

    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'
    testCompile 'junit:junit:4.13.1'

    testCompile 'ch.qos.logback:logback-access:1.2.6'
    testCompile 'ch.qos.logback:logback-classic:1.2.6'
    testCompile 'ch.qos.logback:logback-core:1.2.6'

    testCompile(libs.hutool_log)
    testCompile(libs.fastjson)
    testCompile(libs.jackson_core)
    testCompile(libs.jackson_databind)
    testCompile(libs.jackson_annotations)
    testCompile(libs.jackson_xml)
    testCompile(libs.gson)
    testCompile(libs.avro)
    testCompile(libs.cbor_jackson)
    testCompile(libs.hessian)
    testCompile(libs.kryo)
    testCompile(libs.protobuf_java)
    testCompile(libs.protostuff_core)
    testCompile(libs.fst)
    testCompile(libs.msgpack_jackson)
    testCompile(libs.spearal_java)
    testCompile(libs.thrift)
    testCompile(libs.yaml_jackson)
}