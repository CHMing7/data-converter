dependencies {
    // 引用除了本模块与根模块之外的所有模块
    rootProject.allprojects.stream().filter({
        "data-converter-all" != it.name && it != rootProject && it != project
    }).forEach({
        compile it
    })
}