dependencies {
    compile project(':data-converter-core')
    compile project(':data-converter-codec')

    compileOnly(libs.fst)
    // compileOnly('org.beangle.serializer:beangle-serializer-fst_2.13:0.0.19')

    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'

    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'
    testCompile 'junit:junit:4.13.1'

    testCompile(libs.hutool_log)
    testCompile(libs.fst)
    // testCompile('org.beangle.serializer:beangle-serializer-fst_2.13:0.0.19')
}
